# Coolify gerencia os volumes via Storages no painel.
# Nao defina volumes aqui para evitar conflito.
services:
  yara-report:
    build: .
    ports:
      - "8880:8880"
    environment:
      - HOST=0.0.0.0
      - PORT=8880
    restart: unless-stopped
